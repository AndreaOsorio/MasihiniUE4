static func void run ()
    var b : boolean;
    var i : int;
{
    b = true;
    i = 0;
speak('start');
    while(i<=10){
      speak(i);
    	jump();
    	accel(10.0f);
    	rot(10.0f + 20 * (40+50));
      i = i+1;
    }
speak('it works!');
}